# Local: copy to .env.local. Vercel: Project → Settings → Environment Variables.

# Required to access /admin (set a strong password; never commit the real value).
ADMIN_PASSWORD=

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: Google Sheet endpoint for Stock page sync.
# NEXT_PUBLIC_GOOGLE_SHEET_URL=
